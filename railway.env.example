# Reference: paste these names into Railway → your service → Variables.
# Values shown are examples or empty — fill in the dashboard (or use `railway variables`).
#
# Required
DA_ACTIVITY_ID=
APS_CLIENT_ID=
APS_CLIENT_SECRET=
#
# Optional (defaults are fine for most deploys)
# DA_BUCKET_KEY=
# MAX_UPLOAD_BYTES=83886080
# PLUGIN_BUNDLE_DIR=/app/design_automation/LayerPdfExport/LayerPdfExport.bundle/Contents
# APS_CREDENTIALS_PATH=/app/.aps
